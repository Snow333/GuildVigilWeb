/**
 * The canonical career-harness party — now the shared v1 starter roster in
 * @sim/campaign/starterParty (live play needs it too). This re-export keeps the
 * harness import stable; values are unchanged, so the snapshot is unchanged.
 */

export { mkHero, starterParty, starterParty as fixtureParty } from '@sim/campaign/starterParty';
